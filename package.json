{
  "name": "naran-social-login",
  "description": "Naran social login, just another social login plugin for WordPress.",
  "version": "0.1.0",
  "license": "GPL-2.0-or-later",
  "scripts": {
    "start": "wp-scripts start --output-path=assets/js/dist",
    "build": "wp-scripts build --output-path=assets/js/dist"
  }
}
